import SwiftUI

public extension Image {
    /// ペンギン画像
    static let penguin = Image(.penguin)
    /// お知らせ
    static let information = Image(.information)
    /// 完了
    static let done = Image(.done)
    /// 警告
    static let warning = Image(.warning)
    /// エラー
    static let error = Image(.error)
    /// NoImage
    static let noImage = Image(.no)
}
